let express,readdirSync,cors,mongoose;_e4b‍.w("express",[["default",["express"],function(v){express=v}]]);_e4b‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_e4b‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_e4b‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require('morgan');
require("dotenv").config();

const app = express();

//db connection
mongoose.connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
  })
    .then(() => console.log('DB Connected'))
    .catch((err) => _e4b‍.g.console.log("DB Connection Error: ", err))

//middlewares
app.use(cors());
app.use(morgan("dev"));
app.use(express.json())

//route middleware
readdirSync('./routes').map((r) => app.use('/api', require(`./routes/${r}`)))
//app.use("/api", router);


const port = process.env.PORT || 8000;
app.listen(port, () => {
    console.log(`Server is listening on port ${port}`)
})